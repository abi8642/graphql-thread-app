import {typedef} from "./typedef";
import {queries} from "./queries";
import {mutations} from "./mutations";
import {userResolvers} from "./resolvers";

export const User = { typedef, queries, mutations, userResolvers };